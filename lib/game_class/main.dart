import 'package:escape_game/game_class/lock_system.dart';
import 'package:escape_game/game_class/room.dart';

import 'door.dart';
import 'lock_system.dart';
import 'item.dart';
import 'player.dart';

main() {
  // //TEST PORTE
  //Padlock cadena = Padlock(2, "cadena robuste", "C'est un cadena", "1234");
  // KeyHole serrure = KeyHole(3, "vieille serrure", "C'est une serrure", 3);
  // LockSystem sys = LockSystem(1, "porte1", "C'est un systeme de sécurité", [cadena, serrure]);
  // Door porte1 = Door(3, "porte en bois", "C'est une porte en bois", "salle1", "salle2", true, sys);
  // print(porte1.open());
  // print(porte1.unlock("45"));
  // print(porte1.open());
  // print(porte1.unlock("1234"));
  // print(porte1.open());
  // print(porte1.unlock(2));
  // print(porte1.open());
  // print(porte1.unlock(3));
  // print(porte1.open());
  // print(porte1.watch());

  //Test Papier
  // Paper feuille = Paper(1, "feuille", "C'est une feuille", true, "Je suis le texte de la feuille");
  // Watch montre = Watch(1, "montre", "C'est une montre.");
  // Paper feuille2 = Paper(2, "vieille lettre", "C'est une vieille lettre", true, "Je suis un texte.");
  // Room piece = Room(1, "cuisine", "C'est une cuisine", [], [], [], true, true);
  // Inventory inventaire = Inventory("invenraire", []);
  // Player perso = Player("C'est moi.", piece, inventaire);
  // print(perso.inventory.watch());
  // print(perso.take(feuille));
  // print(perso.take(montre));
  // print(perso.take(feuille2));
  // print(perso.inventory.watch());
}
